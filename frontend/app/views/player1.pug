extends template

block content
    #navbar.right
        a(href='/logout').quiet-link.navbar-item Logout
    h1.title.center Welcome!
        #firstName
    #js-error-message
    p Your Kills:
        #kills
    p Your Current Target:
        #target
block postscript
    script fetchPlayer();
